{
  "name": "colorwizard-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n mcp,web -c cyan,magenta \"npm run dev:mcp\" \"npm run dev:web\"",
    "dev:mcp": "npm run dev:http --workspace apps/mcp",
    "dev:mcp:stdio": "npm run dev:stdio --workspace apps/mcp",
    "dev:web": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/mcp && npm run build --workspace apps/web",
    "test": "npm run test --workspace apps/mcp && npm run test --workspace apps/web --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
